{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run tests",
            "program": "${workspaceFolder}/lib/cmd.js",
            "args": [ ] // Use the same command line arguments as the normal wattle command
        }
    ]
}